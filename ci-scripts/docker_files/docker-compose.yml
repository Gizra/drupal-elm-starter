version: '2'
services:
    hub:
        image: elgalu/selenium
        environment:
          - USE_SELENIUM=2

    server.local:
        image: server
        environment:
            - BUILD_WEBDRIVERIO=1

